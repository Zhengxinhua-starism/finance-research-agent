# ============================================================
# 金融研报 Agent — 依赖清单
# 安装：pip install -r requirements.txt
# Python 版本要求：3.11 ~ 3.12
#   （chromadb / sentence-transformers 在 3.13+ 上尚无稳定 wheel，
#     3.11/3.12 是当前依赖组合的安全区间）
# ============================================================

# ---------------- LLM 调用 ----------------
httpx>=0.27,<0.29
openai>=1.40

# ---------------- Agent 编排框架 ----------------
langgraph>=0.2,<0.4
langchain-core>=0.3,<0.4

# ---------------- 向量数据库 ----------------
chromadb>=0.5,<0.7

# ---------------- Embedding + Rerank + BM25 ----------------
sentence-transformers>=3.0
rank-bm25>=0.2.2
# CPU 版 torch 体积更小；如需 GPU 请自行按官方指引安装对应 CUDA 版本
torch>=2.2

# ---------------- 金融数据 ----------------
akshare>=1.14
pandas>=2.0
numpy>=1.26

# ---------------- Web 框架 ----------------
fastapi>=0.115
uvicorn[standard]>=0.32
# FastAPI 同步端点转线程池 / anyio.to_thread 依赖
anyio>=4.4

# ---------------- Redis ----------------
redis>=5.0

# ---------------- 数据类 / 配置 ----------------
pydantic>=2.7
pydantic-settings>=2.3

# ---------------- 工具 ----------------
python-dotenv>=1.0
tiktoken>=0.8
tenacity>=8.3

# ---------------- 测试 ----------------
pytest>=8.0
